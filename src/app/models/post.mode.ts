export interface Post {
    title: string;
    category: string;
    description: string;
}

export interface Contact {
    name: string;
    email: string;
    subject: string;
    message: string;
}

export interface User {
    studentEmail: string;
    password: string;
}